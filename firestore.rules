rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all access for debugging (REMOVE IN PRODUCTION!)
    match /{document=**} {
      allow read, write: if true;
    }
  }
} 